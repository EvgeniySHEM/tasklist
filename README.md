An application for maintaining a list of tasks.

Spring Boot application for REST API with support for PostgreSQL, Spring Security (JWT), securityexpressions, JDBC, Mybatis, Liquibase, Swagger, Docker.
